<!--pages/category/goods/goods.wxml-->
<view class='goods'>
<navigator url='/pages/goods/detail?goodid={{item.id}}' wx:for='{{goodsList}}' wx:key='{{item.id}}'>
  <view class='one_goods'>
    <view class='img'>
      <image class='goods_img' src='{{item.image}}'></image>
    </view> 
    <text class='title'>{{item.goods_name}}</text>
    <text class='subtitle'>{{item.title}}</text>
    <text class='price'>￥{{item.id}}</text>
  </view>
</navigator>
  
</view>


